{
	"scripts": {
		"php-server": "cd ./public/ && php -S localhost:80",
		"docker-server": "docker compose up",
		"docker-daemon-up": "docker compose up -d",
		"docker-daemon-down": "docker compose down"
	}
}